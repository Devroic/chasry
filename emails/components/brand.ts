/**
 * Email-safe brand palette in hex, since email clients don't get CSS vars. Mirrors the
 * app/globals.css light-mode tokens.
 */
export const EMAIL_BRAND = {
  primary: "#23458D",
  ink: "#0D0D0D",
  neutral: "#5B6B85",
  tint: "#E2E9F8",
  border: "#E3E8F2",
} as const;
